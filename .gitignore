# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

stubs/.env.total
stubs/.env.example.final
stubs/.env.tmp

# Test binary, build with `go test -c`
*.test
coverage.txt

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
.idea
.DS_Store
